- type: filestream
  id: "{{ subspace_cache_binary_name }}"
  paths:
    - "{{ subspace_cache_log_folder }}/*.log"
  processors:
    - add_tags:
        tags: subspace_cache
        target: subspace_app
